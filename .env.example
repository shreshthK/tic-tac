# ============================================
# Tic-Tac-Toe Application Environment Variables
# ============================================
# Copy this file to .env and fill in your values
# NEVER commit .env files with real secrets to git
# ============================================

# ----------------
# Application Mode
# ----------------
NODE_ENV=production
# Options: development, production, test

# ----------------
# Backend Configuration
# ----------------
# Port the backend server listens on (inside container)
PORT=3000

# SQLite database file path
# In Docker: /app/data/data.db (mounted volume)
# In development: ./data.db (relative to backend directory)
DATABASE_PATH=/app/data/data.db

# CORS allowed origins (comma-separated for multiple)
# Leave empty to allow all origins in development
# In production, set to your domain: https://yourdomain.com
CORS_ORIGIN=

# ----------------
# Frontend Configuration
# ----------------
# These are set at build time via Vite
# The frontend auto-detects WebSocket URL from window.location
# No additional env vars needed for basic setup

# ----------------
# AWS Configuration (for CI/CD)
# ----------------
# These should be set as GitHub Secrets, not in this file
# Listed here for documentation purposes only

# AWS_ACCESS_KEY_ID=your-access-key-id
# AWS_SECRET_ACCESS_KEY=your-secret-access-key
# AWS_REGION=us-east-1
# EC2_INSTANCE_ID=i-0123456789abcdef0

# ----------------
# SSH Configuration (Alternative to SSM)
# ----------------
# EC2_HOST=your-ec2-public-ip
# EC2_USER=ubuntu
# EC2_SSH_KEY=contents-of-your-pem-file

# ----------------
# Optional: Monitoring & Logging
# ----------------
# LOG_LEVEL=info
# Options: error, warn, info, debug

# ----------------
# Optional: Rate Limiting
# ----------------
# RATE_LIMIT_WINDOW_MS=60000
# RATE_LIMIT_MAX_REQUESTS=100

# ----------------
# Optional: SSL/TLS (when using HTTPS)
# ----------------
# SSL_CERT_PATH=/etc/nginx/ssl/fullchain.pem
# SSL_KEY_PATH=/etc/nginx/ssl/privkey.pem
